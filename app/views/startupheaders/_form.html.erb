<section id="formBacker">
	<div class="container">
		
		<div id="cmsHeader">
			<h2 id="cmsTitle">Add/edit your startup section header and strapline here</h2>
		</div>
		
		<div class="formWrapper">
			<% if @startupheader.errors.any? %>
				<div class="message warning">
					<% @startupheader.errors.full_messages.each do |msg| %>
			   			<%= msg %><br/>
					<% end %>
				</div>
			<% end %>	
			<%= form_for(@startupheader) do |f| %>
				<%= f.label :Title, class: "formText" %><br/>
    			<%= f.text_field :title, class: 'text-area' %><br /><br />
				<%= f.label :Strapline, class: "formText" %><br/>
	    		<%= f.text_area :strapline, class: 'text-areaArea' %><br /><br />
	    		<%= f.submit %>
			<% end %>
		</div>
		
	</div>
</section>