<section id="formBacker">
	<div class="container">
		
		<div id="cmsHeader">
			<h2 id="cmsTitle">Add/edit your about top block content here</h2>
		</div>
		
		<div class="formWrapper">
			<% if @abouttb.errors.any? %>
				<div class="message warning">
					<% @abouttb.errors.full_messages.each do |msg| %>
			   			<%= msg %><br/>
					<% end %>
				</div>
			<% end %>	
			<%= form_for(@abouttb) do |f| %>
				<%= f.label :Title, class: "formText" %><br/>
    			<%= f.text_area :title, class: 'text-area' %><br /><br />
				<%= f.label :Description, class: "formText" %><br/>
	    		<%= f.text_area :content, class: 'text-areaArea' %><br /><br />
	    		<%= f.submit %>
			<% end %>
		</div>
		
	</div>
</section>